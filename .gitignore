# Chezmoi
.chezmoi*.yaml

# macOS
.DS_Store

# Secrets - never commit these
*secret*
*private*
*.pem
*.key
# chezmoi uses private_ prefix for 600-permission targets (content is a template, not a secret)
!*private*.tmpl

# Backup files
*.bak
*~
